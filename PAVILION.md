# 巴塞罗那德国馆：增量精修与验收记录

保留原项目的 React / Three.js / Vinext 技术和数据结构，没有更换项目或依赖锁文件。

## 上一轮模型与交互精修

- 删除室内漫游、自动导览、路径寻路及相应状态；英文标题不再附年份。
- `components/pavilion/plan.ts`：统一地坪标高、层高、池深、柱断面、台阶参数；修补水院接缝及围墙端点，补齐官方平面可确认的后侧平台。
- `model.ts`：柱脚、十字柱腹板和翼缘无实体重叠；屋面与四边收边分开建模；两个水池均有池底、池壁和低于平台的水面；调整家具金属支架；移除无依据的树木。
- `materials.ts`：按空间尺度设置 UV，四张 512² 程序纹理；区分洞石、绿石、缟玛瑙、金属、玻璃、水和软包的物理参数。
- `config.ts`：集中管理相机、自然光、画质、模式、动画和分解层级。
- `engine.ts`：以同一个状态源驱动界面与模型；模式互斥，分解转剖切须先恢复构件，顶视自动还原；动画最终精确归零；隐藏页面停止绘制，静止时不持续渲染；资源与监听器统一释放。
- `software-renderer.ts`：无 WebGL 2 时使用同一模型、相机、裁剪面的 CPU 深度缓冲显示。界面明确标注轻量模式，不假称写实渲染。
- `Exhibition.tsx`、`webmcp.ts`、`app/globals.css`、`app/layout.tsx`、`components/ui/slider.tsx`：四模式工具栏、字号与触控尺寸、真实加载状态、键盘纯净展示、可访问滑杆、响应式布局及保留的结构化操作接口。

## 实际验证（2026-09-16）

| 项目 | 结果 |
| --- | --- |
| TypeScript 类型检查 | 通过，无错误 |
| 生产构建 | 通过；Three.js 模块触发大于 500 kB 的分块提示，未隐藏该提示 |
| 几何检查 `node scripts/check-pavilion.mjs` | 通过：8 根柱、2 个水池、10 组构件；130 个结构实体、8,385 对包围盒无体积重叠；柱脚/屋面接触、水面标高、玻璃位于屋面范围内检查通过 |
| 鼠标拖动、Shift+拖动平移、滚轮和加减按钮 | 实际操作后相机发生对应变化；复位成功 |
| 整体、入口、水池、庭院、顶视 | 五个预设实际切换；顶视使用正交相机 |
| 打开屋顶、再次合顶 | 屋面最终偏移 6.5，合顶最终偏移严格为 0 |
| 建筑分解、滑杆 0/100、还原按钮 | 通过；墙、玻璃、柱、屋面分为不同高度，0 时准确装配 |
| 剖切位置 0/60/100，纵/横方向 | 通过；场景背景不参与裁剪；从分解切换须先归零再启用裁剪 |
| 连续开顶→分解→剖切→顶视 | 通过，最终外观模式、无剖切、所有偏移为 0 |
| 构件拾取、隐藏屋面、显示全部 | 实际操作通过，顶视可见室内布置 |
| 平面导航展开、定位、收起 | 通过 |
| 关于建筑面板打开/关闭 | 通过，参考链接与示意重建说明保留 |
| 纯净展示、退出按钮、Esc/H | 通过；界面隐藏，模型继续可操作 |
| 刷新 | 默认状态正确恢复，无加载遮罩残留 |
| 尺寸 | 1363×936 桌面；使用真实 iframe 视口检查 1024×768、844×390、390×844；修复竖屏相机距离上限导致的截断。临时测试容器已移除 |
| 页面控制台 | 未发现页面来源的错误或警告；浏览器扩展自身报错不属于项目 |

## 验证边界

测试浏览器禁用 WebGL，所以上述浏览器验证运行于明确标注的同几何轻量模式。真实 GPU 的材质色调、曝光、玻璃反射排序、水面反射、阴影、帧率与两个画质档位的显示效果**尚未完成实机验收**。轻量模式不显示画质选择器。不能把本记录解释为 GPU 写实画面的完整验收。

当前浏览器不提供 WebMCP modelContext，三个接口已保留并通过类型检查，但无法在此浏览器执行接口验收；手动界面交互独立有效。

## 本轮光影与材质精修（2026-09-16）

范围：保留建筑构件尺寸、位置、页面布局、文字、相机预设和交互状态逻辑；未增加依赖。实体几何仅增加表面细分，用于接触遮蔽插值。`plan.ts`、`Exhibition.tsx`、`webmcp.ts`、`app/`、依赖与锁文件未修改。

定位到的主要问题：半球光过强、浅色材质与背景明度接近；纹理只有颜色而缺少独立粗糙度/法线；环境太阳方向与直射日光不一致；缺少构件交接处的局部遮蔽。

| 文件 | 修改 |
| --- | --- |
| `config.ts` | 曝光 0.90→0.73、半球光 1.70→0.62、环境强度 0.60→0.38；集中日光方向、阴影和表面细节参数 |
| `materials.ts` | 四套 512² 程序 PBR 纹理，分别提供 sRGB 颜色与线性粗糙度/法线；洞石孔隙、石纹、金属粗糙度、玻璃边缘反射；地面边缘柔和融入背景 |
| `contact-lighting.ts`（新增） | 利用现有实体预烘焙短距离环境遮蔽，仅影响间接光；开顶/分解时淡出，剖切/隐藏构件时停用，避免残影 |
| `rendering.ts`（新增） | 暖灰渐变背景；按距离、窗口宽度和画质选择阴影/反射分辨率，带阈值滞回避免反复分配 |
| `model.ts` | 保持池壁、池底、水深和轮廓；统一环境太阳方向；水面 Fresnel 倒影与克制的静态细波纹；反射捕获时排除水膜及其他镜面，避免递归 |
| `engine.ts` | 保留 ACES 与 sRGB；稳定日光阴影范围与按需更新；距离相关法线强度与铺装分缝；记录渲染耗时供诊断 |
| `software-renderer.ts` | 无 WebGL 时同几何轻量显示也使用石材颜色纹理、烘焙明暗与色调映射；缓存几何、纹理 mipmap，转场降低像素数，静止恢复细节 |
| `scripts/check-pavilion.mjs` | 增加纹理色彩空间、着色器插入点、AO 数值范围和渲染预算检查 |

本轮实际验收：整体、入口、水池、庭院、顶视、开顶、分解、纵/横剖切及位置滑杆均实际切换并查看画面；连续开顶→分解→剖切→复位通过，构件归零、裁剪关闭、遮蔽恢复；纯净展示与退出、刷新通过。桌面 1363×936，以及真实 iframe 视口 1024×768、844×390、390×844 加载通过，竖屏采用移动端远景预算。页面来源控制台错误/警告为 0（排除浏览器扩展自身错误）。

`tsc --noEmit`、生产构建、几何/渲染契约检查通过。61,409 个顶点；130 个结构实体、8,385 对检查无体积重叠。构建保留 Three.js 分块超过 500 kB 的提示。

验收仍运行在软件后备模式。其转场降低分辨率，静止按需绘制；不同视口的单帧 CPU 记录约 45–115 ms，不能称为 60 fps，也不能据此推断 GPU 性能。实际 WebGL 的着色器编译、透明排序、反射质量、曝光、阴影闪烁及实机帧率仍未验证；着色器源码契约检查不等于 GPU 编译测试。

仍属近似：石材是程序纹理而非现场扫描；AO 是短距离静态近似，开顶时不保留部分构件的 AO；水纹静态、玻璃用透明度与 Fresnel 表现而非物理折射。建筑原有示意重建边界不变。

## 资料与重建范围

- [Fundació Mies van der Rohe 建筑与材料介绍](https://miesbcn.com/the-pavilion/)
- [基金会现状平面图](https://miesbcn.com/wp-content/uploads/2026/02/Mies_plano_A3PNG-1-scaled.png)
- [基金会场地与屋顶图](https://miesbcn.com/wp-content/uploads/2014/04/planol-entorn.pdf)

模型依据公开平面比例和实景，属于示意重建。总尺度、墙厚、屋面构造、池深、附属空间细节未经测绘确认。石材为程序纹理，家具支架和雕塑人体保守简化；不包含地下设备与场外景观测绘。分解不是施工顺序，家具/雕塑剖切不作实体封口。几何无交叉检查不能代替历史真实性或工程精度认证。
